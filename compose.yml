services:
  backend:
    image: coversongs-backend:latest
    container_name: backend
    ports:
      - 5286:8080

  frontend:
    image: coversongs-frontend:latest
    container_name: frontend
    ports:
      - 8080:80